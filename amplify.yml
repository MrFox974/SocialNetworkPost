version: 1
applications:
  - appRoot: frontend
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
          env:
            - VITE_PROTOCOLE: https
            - VITE_SERVER_HOST: $VITE_SERVER_HOST
            - VITE_SERVER_PORT: 
            - VITE_AUTH_PRODUCTION: $VITE_AUTH_PRODUCTION
            - VITE_GOOGLE_CLIENT_ID: $VITE_GOOGLE_CLIENT_ID
      artifacts: 
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
